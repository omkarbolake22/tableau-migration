FROM python:3.11-slim

WORKDIR /app

# Copy everything (you can adjust later)
COPY . /app

# No requirements, skip pip install

# Default command does nothing but keeps container alive
CMD ["python", "-c", "print('Container ran successfully!')"]
